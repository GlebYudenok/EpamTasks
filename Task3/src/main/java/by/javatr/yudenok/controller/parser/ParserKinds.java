package by.javatr.yudenok.controller.parser;

public enum ParserKinds {
    /**
     * kind of parser
     * enum element for factory.
     */
    JAXB,
    /**
     * kind of parser
     * enum element for factory.
     */
    DOM,
    /**
     * kind of parser
     * enum element for factory.
     */
    StAX,
    /**
     * kind of parser
     * enum element for factory.
     */
    SAX
}
